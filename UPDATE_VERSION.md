# 📋 Інструкція: Як оновити версію додатку

## ⚠️ ВАЖЛИВО: При кожному оновленні коду треба змінювати версію!

### Крок 1: Оновіть версії в файлі `public/sw.js`

Відкрийте файл `public/sw.js` та збільште обидві версії:

```javascript
const CACHE_VERSION = 'v3';  // ← Змініть це (v3 → v4, v4 → v5, і т.д.)
const APP_VERSION = '1.0.3'; // ← І це (1.0.3 → 1.0.4, 1.0.4 → 1.0.5, і т.д.)
```

### Крок 2: Збережіть файл та задеплойте

```bash
# Запуск локально для тестування
npm run dev

# Або деплой на Vercel
git add .
git commit -m "Update version to X.X.X"
git push
```

## 🔄 Що відбувається при оновленні версії:

1. **Service Worker виявляє нову версію** - коли користувач відкриває сайт, браузер перевіряє чи змінився файл `sw.js`
2. **Автоматичне видалення старого кешу** - всі старі кешовані файли видаляються
3. **Примусове перезавантаження сторінки** - всі відкриті вкладки автоматично перезавантажуються через 1-2 секунди
4. **Сповіщення працюють знову** - новий Service Worker отримує всі дозволи на сповіщення

## 🧪 Як перевірити що оновлення працює:

1. Відкрийте сайт у браузері
2. Відкрийте DevTools (F12) → вкладка Console
3. Змініть версію в `sw.js`
4. Задеплойте зміни
5. Через 30 секунд в консолі побачите:
   ```
   🔄 Перевірка оновлень SW...
   🔄 Знайдено нову версію Service Worker
   🔄 Нова версія встановлена!
   🔄 Автоматичне перезавантаження...
   ```
6. Сторінка автоматично перезавантажиться
7. Внизу сторінки побачите нову версію: "Версія: 1.0.X"

## 🐛 Проблеми та рішення:

### Проблема: Сповіщення не працюють після оновлення
**Рішення:** Збільште `CACHE_VERSION` та `APP_VERSION`, задеплойте. Стара версія SW буде повністю видалена.

### Проблема: Бачу стару версію додатку
**Рішення:**
1. Перевірте що змінили обидві версії в `sw.js`
2. Очистіть кеш браузера вручну: DevTools → Application → Clear storage
3. Перезавантажте сторінку

### Проблема: Оновлення не відбувається автоматично
**Рішення:**
1. Перевірте консоль на помилки
2. Видаліть Service Worker вручну: DevTools → Application → Service Workers → Unregister
3. Перезавантажте сторінку

## 📝 Історія версій:

- **v3 / 1.0.3** - Додано агресивне примусове оновлення та автоматичне перезавантаження
- **v2 / 1.0.2** - Додано базове оновлення Service Worker
- **v1 / 1.0.1** - Перша версія з PWA підтримкою
