# Оновлення сповіщень PWA - v1.0.4

## Зміни в цьому оновленні:

### ⏰ Час оновлення
- **Змінено** інтервал перевірки з 1 хвилини на **5 хвилин** (300000 мс)
- Це зменшує навантаження на сервер та батарею пристрою

### � Відображення графіка Tomorrow
- **НОВА ФУНКЦІЯ**: Додано окремий блок для відображення графіка на завтра
- Графік Tomorrow тепер завжди показується внизу сторінки (якщо він є)
- Якщо Tomorrow немає - показується повідомлення "📭 Ще немає графіка на завтра"
- Графік Today залишається основним вгорі сторінки
- Працює з mobile/desktop версіями зображень та HTML

### �🔔 Покращення сповіщень

#### У файлі app/page.tsx:
- **Додано** `requireInteraction: true` - сповіщення залишається на екрані до закриття користувачем
- **Додано** `silent: false` - сповіщення НЕ тихе, буде з системним звуком
- **Додано** унікальний `tag` для кожного сповіщення - `schedule-update-${Date.now()}`
- **Додано** налаштування мови `lang: 'uk'` та напрямку `dir: 'ltr'`

#### У файлі public/sw.js:
- **Оновлено** версію з v3/1.0.3 до v4/1.0.4
- **Додано** `requireInteraction: true` - показувати до закриття користувачем
- **Додано** `silent: false` - НЕ тихий режим, з системним звуком
- **Додано** `vibrate: [200, 100, 200, 100, 200]` - вібрація для мобільних пристроїв
- **Додано** `timestamp: Date.now()` - час сповіщення
- **Додано** налаштування мови та напрямку тексту
- **Додано** дані для обробки кліку на сповіщення

### 📱 Покращена підтримка мобільних пристроїв
- Сповіщення тепер краще працюють на Android
- Додано вібрацію для сповіщень
- Покращена видимість сповіщень у шторці

## Як перевірити роботу:

1. **Відкрийте додаток** у браузері
2. **Дозвольте сповіщення** якщо ще не дозволили
3. **Натисніть кнопку "Тестувати сповіщення"** - має з'явитися:
   - 🔔 Звуковий сигнал (з обраного звуку)
   - 📳 Вібрація (на мобільних)
   - 🔔 Банер сповіщення (має залишатися до закриття)
4. **Прокрутіть вниз** - побачите блок "📅 Графік на завтра":
   - Якщо є Tomorrow - покаже графік
   - Якщо немає - покаже "📭 Ще немає графіка на завтра"
5. **Зачекайте 5 хвилин** - додаток автоматично перевірить оновлення

## Відстеження змін:

### Сповіщення показуються коли:
- ✅ Змінився графік відключень (Today або Tomorrow)
- ✅ Змінився архів графіків (додано/видалено/змінено)
- ✅ З'явився новий графік
- ✅ Зник існуючий графік

### Звук відтворюється коли:
- ✅ Показується сповіщення про зміни
- ✅ Користувач натискає "Тестувати сповіщення"
- ✅ Користувач надає дозвіл на сповіщення (тестове сповіщення)

## Налаштування звуку:
У додатку є вибір з 3 звуків:
- 🔔 Звук 1 (Стандартний)
- 🔔 Звук 2 (Довгий)
- 🔔 Звук 3 (Тихий)

Вибраний звук зберігається в `localStorage` і не втрачається після перезавантаження.

## Технічні деталі:

### Service Worker
- Використовується для показу сповіщень (краще для Android)
- Автоматично оновлюється кожні 30 секунд
- Видаляє старі кеші при оновленні

### Перевірка змін
- Використовується хешування для точної перевірки змін
- Окремо відстежуються зміни графіків та архіву
- Перше завантаження НЕ показує сповіщення

## Відомі обмеження:

### iOS Safari:
- PWA сповіщення мають обмежену підтримку
- Звук може не працювати через обмеження системи
- Вібрація не підтримується

### Desktop:
- Вібрація не працює (тільки для мобільних)
- Звук може залежати від системних налаштувань

### Загальні:
- Потрібен дозвіл користувача на сповіщення
- Додаток повинен бути відкритим у фоні (або Service Worker активний)

## Що робити якщо сповіщення не працюють:

1. **Перевірте дозвіл** - має бути "granted" (зелений текст біля кнопки)
2. **Перевірте консоль** браузера (F12) - там будуть детальні логи
3. **Протестуйте** кнопкою "Тестувати сповіщення"
4. **Перезавантажте** додаток (закрийте та відкрийте знову)
5. **Очистіть кеш** та перезареєструйте Service Worker

## Логи в консолі:

Корисні логи для діагностики:
- `🔔 Поточний дозвіл на notifications:` - статус дозволу
- `✅ Service Worker зареєстровано` - SW працює
- `🔔 ГРАФІК ЗМІНИВСЯ!` - виявлено зміни, показується сповіщення
- `✅ Notification показано` - сповіщення успішно створено
- `🔊 Звук відтворено успішно` - звук працює

## Дата оновлення:
13 грудня 2025

## Версія:
v1.0.4
